import { getCategories } from './categories';
import { getPosts, addPost, editPost, deletePost, upvotePost, downvotePost } from './posts';
import { getComments, addComment, editComment, deleteComment, upvoteComment, downvoteComment } from './comments';

export default {
  getCategories,
  getPosts,
  addPost,
  editPost,
  deletePost,
  upvotePost,
  downvotePost,
  getComments,
  addComment,
  editComment,
  deleteComment,
  upvoteComment,
  downvoteComment
}